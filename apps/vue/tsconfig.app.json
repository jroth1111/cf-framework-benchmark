{
  "extends": "@vue/tsconfig/tsconfig.dom.json",
  "include": ["env.d.ts", "src/**/*", "src/**/*.vue"],
  "exclude": ["src/**/__tests__/*"],
  "compilerOptions": {
    // Extra safety for array and object lookups, but may have false positives.
    "noUncheckedIndexedAccess": true,
    "ignoreDeprecations": "6.0",
    "baseUrl": ".",
    "lib": ["ES2023", "DOM", "DOM.Iterable"],

    // Path mapping for cleaner imports.
    "paths": {
      "@/*": ["./src/*"],
      "@cf-bench/dataset": ["../../packages/dataset/src/index.d.ts"],
      "@cf-bench/chart-core": ["../../packages/chart-core/src/index.d.ts"],
      "@cf-bench/bench-types": ["../../packages/bench-types/src/index.ts"],
      "@cf-bench/ui/styles.css": ["../../packages/ui/src/styles.css"]
    },

    // `vue-tsc --build` produces a .tsbuildinfo file for incremental type-checking.
    // Specified here to keep it out of the root directory.
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo"
  }
}
