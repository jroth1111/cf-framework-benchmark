{
  "name": "cf-bench-react-vite",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node ./scripts/generate-pages.mjs",
    "dev": "pnpm run generate && vite --port 5173",
    "build": "pnpm run generate && vite build",
    "preview": "pnpm run build && wrangler dev --port 8787 --inspector-port 9230",
    "deploy": "pnpm run build && wrangler deploy"
  },
  "dependencies": {
    "@cf-bench/bench-config": "workspace:*",
    "@cf-bench/bench-types": "workspace:*",
    "@cf-bench/chart-core": "workspace:*",
    "@cf-bench/chart-hooks": "workspace:*",
    "@cf-bench/dataset": "workspace:*",
    "@cf-bench/ui": "workspace:*",
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react-swc": "^4.2.2",
    "typescript": "^5.9.3",
    "vite": "^7.3.0",
    "wrangler": "^4.56.0"
  }
}
